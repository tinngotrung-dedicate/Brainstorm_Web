GEMINI_API_KEY=your-gemini-key
# Disable file persistence by default for Vercel/serverless
ENABLE_PERSISTENCE=false
DATA_DIR=/tmp/brainstorm-store
# Optional: allow Google Scholar scraping (requires python3 + bs4)
ENABLE_SCHOLAR=false
